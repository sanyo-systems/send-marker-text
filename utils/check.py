from datetime import datetime, time, timedelta

# ==========================================================
# 時間帯判定
#
# 現在時刻の hour をそのまま返す。
#
# 以前は毎時00分からの時間制限があったが、
# 現在は時刻に関係なく入力できる運用とする。
# ==========================================================
def check_time():
    return str(datetime.now().hour)
